{
  "name": "findash",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm install && cd client && npm install && npm run build && cd .. && cd server && npm install && cd ..",
    "start": "npm run start --prefix server & npm run start --prefix client"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
